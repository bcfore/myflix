- @categories.each do |category|
  %article.video_category
    %header
      %h3= link_to category.name, category
    = render 'videos', videos: category.recent_videos

-# - @categories_videos.each do |category, videos|
-#   %article.video_category
-#     %header
-#       %h3= link_to category.name, category
-#     = render 'videos', videos: videos
