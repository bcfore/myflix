%section.my_queue.container
  .row
    .col-sm-10.col-sm-offset-1
      %article
        %header
          %h2 My Queue
        %table.table
          %thead
            %tr
              %th(width="10%") List Order
              %th(width="30%") Video Title
              %th(width="10%") Play
              %th(width="20%") Rating
              %th(width="15%") Genre
              %th(width="15%") Remove
          %tbody
            - @queue.each do |row|
              %tr
                %td
                  %input.form-control(type="text" value="#{row.place_in_queue}")
                %td
                  = link_to row.video_title, row.video
                %td
                  = button_to "Play", nil, class: "btn btn-default"
                %td
                  #{row.rating}
                  -# %select.form-group(name="")
                  -#   %option(value="5") 5 Stars
                  -#   %option(value="4") 4 Stars
                  -#   %option(value="3") 3 Stars
                  -#   %option(value="2") 2 Stars
                  -#   %option(value="1") 1 Star
                %td
                  = link_to row.category_name, row.category
                %td
                  = link_to my_queue_video_path(row.video), method: :delete do
                    %i.glyphicon.glyphicon-remove
        = button_to "Update Instant Queue", nil, class: "btn btn-default"
